export var color = 'red';
export let name = 'Charles';
export const MAGIC_NUMBER = 7;

export function plus(a, b) {
	return a + b;
}

export class Rectangle {
	constructor(length, width) {
		this.length = length;
		this.width = width;
	}
}